services:
  web:
    build:
      context: ./images
      dockerfile: DockerFile
    image: php-apache
    ports:
      - "8080:80"
    volumes:
      - ./php:/var/www/html/php
      - ./js:/var/www/html/js
      - ./public:/var/www/html
    environment:
      - DB_HOST=db
      - DB_NAME=banderes_quiz
      - DB_USER=usuari
      - DB_PASS=paraula_de_pas
    depends_on:
      - db

  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_DATABASE: banderes_quiz
      MYSQL_USER: usuari
      MYSQL_PASSWORD: paraula_de_pas
      LANG: C.UTF-8
    volumes:
      - ./db_data:/var/lib/mysql
      - ./db_init:/docker-entrypoint-initdb.d

  adminer:
    image: adminer
    ports:
      - "8081:8080"
    depends_on:
      - db
